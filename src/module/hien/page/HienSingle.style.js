import createStyles from '@material-ui/core/styles/createStyles';
const hienStyle = (theme) => createStyles({
    container: {
        padding: theme.spacing.unit * 10,
    },
});
export default hienStyle;
